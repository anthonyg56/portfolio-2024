import { Album } from "@/lib/types"
import { useState } from "react"


export default function PageContent() {
  const [view, setView] = useState<Album | null>(null);

  return (
    <div>

    </div>
  )
}